// @muen/dsh-muen-plugins — host half. Minimal: registers nothing (the Settings
// tab is rendered entirely by ./client.js). Kept as a stable loader entry.
export const name = 'muen-plugins'
export const inject = []
export function apply() {}
